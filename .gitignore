node_modules
dist
.env
.env.*
!.env.example
.vercel
.netlify
*.local

.vercel
pinnacle-office/state
pinnacle-office/reports
qa-reports

# Pinnacle voice settings. Never committed: it can hold an API key.
pinnacle-office/voice.json

# CBSE source PDFs (sample papers, marking schemes, PYQs). CBSE's material,
# not ours, and hundreds of MB of it. scripts/sources/manifest.mjs is committed
# instead, so `node scripts/sources/fetch.mjs` rebuilds this from scratch.
sources/
