# spacing and layout grid

What the frontend specialists in this seat have learned working on Pinnacle AI.
- Tutor.tsx only has one vh-based height rule at the root flex container (line 345); everything else inside inherits height via flex, so a single dvh swap covers the whole panel.
